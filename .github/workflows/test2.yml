name: TestSSh2

on:
  pull_request:
    branches: [ "master" ]

jobs:

  sshConnect:
    runs-on: ubuntu-latest
    steps:
      - name: Connect
        run: |
          echo '${{vars.SSH_KEY}}'
          touch /home/runner/.ssh/test_rsa
          echo '${{vars.SSH_KEY}}' >> /home/runner/.ssh/test_rsa
          ssh -p 2229 -v ${{vars.SSH_USER}}@${{vars.SSH_HOST}}
          echo connected successfully
